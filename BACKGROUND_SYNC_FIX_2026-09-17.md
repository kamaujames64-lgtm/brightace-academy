# BrightAce Background Synchronization Fix — 2026-09-17

Only automatic polling behavior was changed. Periodic data synchronization now runs while a page is hidden, so visible forms, dashboards, conversations, and work areas are not overwritten by scheduled refreshes. Existing manual refresh controls and existing visibility-change synchronization remain intact. No unrelated application behavior was changed.
